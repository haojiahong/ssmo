<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hjh.ssmo.mapper.blog.TagMapper">

	<select id="getTagCount" parameterType="map" resultType="int">
		select count(1) from t_tag
		where 1 = 1
		<if test="null != tag.tagName and '' != tag.tagName">
			<![CDATA[ and tagName like concat('%', #{tag.tagName}, '%')]]>
		</if>
	</select>

	<select id="getTagList" parameterType="map" resultType="tag">
		select * from t_tag
		where 1 = 1
		<if test="null != tag.tagName and '' != tag.tagName">
			<![CDATA[ and tagName like concat('%', #{tag.tagName}, '%')]]>
		</if>
		limit #{start},#{limit}
	</select>

	<select id="getAllTagList" parameterType="map" resultType="tag">
		select *
		from t_tag
		where 1 = 1
	</select>

	<select id="getTagById" parameterType="string" resultType="tag">
		select * from t_tag where id = #{id}
	</select>

	<update id="editTag" parameterType="tag">
		update t_tag
		set tagName =
		#{tagName}
		where id = #{id}
	</update>

	<insert id="addTag" parameterType="com.hjh.ssmo.model.blog.Tag">
		<![CDATA[
			insert into t_tag 
			(tagName) 
				values 
			(#{tagName})
		]]>
		<selectKey resultType="int" keyProperty="id">
			<![CDATA[
			SELECT LAST_INSERT_ID() as id 
			]]>
		</selectKey>
	</insert>

	<delete id="deleteTag" parameterType="string">
		delete from t_tag where id
		= #{id}
	</delete>

	<select id="getArticleTagList" parameterType="string"
		resultType="Tag">
		select ta.* from t_article_tag aat
		inner join t_tag ta on
		aat.tagId = ta.id
		where aat.articleId = #{id}
	</select>
</mapper>
